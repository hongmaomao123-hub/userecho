# 决策日志

每做一个设计取舍就记一条。这是面试时回答「为什么这样做」的素材。

| 日期 | 决策 | 考虑过的方案 | 为什么选这个 |
|---|---|---|---|
| 2026-09-22 | 用固定主题表，不让 LLM 开放命名主题 | 开放聚类；LLM 自由命名 | 主题名固定才能计算 Topic Accuracy，结果也可复现 |
| 2026-09-22 | 统计和优先级由代码计算 | 交给 LLM 一起算 | LLM 算数不可靠，而且不可复现 |
| 2026-09-22 | 澄清 `risk_alert` 是独立风险标志，不是 P0–P3 优先级：任意 negative/mixed 的 safety_discomfort 或任意 severity=3 均设置 `risk_alert=true`；负向提及数 ≥2 时正常计算 P0–P3，可同时存在风险标志；<2 且高风险时不计算 P0–P3，只生成风险标志并进入人工复核；<2 且非高风险时不计算 P0–P3、不生成风险标志，只标记 `insufficient_evidence` | 将风险警报与优先级设为互斥输出；仅低频问题触发风险警报 | 将风险识别与排序证据门槛分开，避免高频风险漏报，也避免低频反馈被赋予缺乏证据支持的优先级；风险召回指标与该定义保持一致，低频项按实际证据展示 |
| 2026-09-22 | 统一 eval 访问说明：步骤1–7禁止访问，步骤8仅允许评测脚本读取；编码助手默认只看汇总指标，不根据 eval 原文修改 prompt | 目录说明笼统写为「禁止访问」 | 与既有评测规则一致，明确最终评测的读取边界 |
| 2026-09-22 | 项目负责人于 2026-09-22 经产品判断明确批准 v1.3 正式字段契约：`raw_sample_size`、`valid_sample_size`、`analysis_mode`（blocked / summary_only / exploratory / standard）；文档与实现均遵循该正式契约 | 将代码回退到 PRD 残留的旧字段和枚举 | 这是正式产品决策，不是为了迁就代码临时修改 PRD；更新后的 PRD 仍为唯一有效版本，今后实现与 PRD 冲突时，必须先提出变更并等待确认，不得自行修改 PRD 迁就代码 |
| 2026-09-22 | 完整读取和清洗 CSV 后，仅返回前 200 条有效反馈；`valid_sample_size` 等于最终 `cleaned_df` 行数，最大为 200；警告显示截断前有效总数、实际处理数、未处理数和原始排序影响 | 延续此前全量返回的决定；静默截断；读取原始前 200 行后再清洗 | 本次产品决策替代此前全量处理口径，限制后续处理规模，同时避免清洗不足及对用户隐瞒未处理数据；文本和评分统计也以最终返回数据为准 |
| 2026-09-22 | 先按 ID 保留第一条，再排除空 ID（计入 `empty_id_count`），再排除空文本；真正缺失的 CSV 单元格为空文本，字面「NA」「NULL」保留；依次尝试 utf-8-sig 和 gb18030 | 将 NA/NULL 自动识别为缺失；撤销空 ID 清洗；使用原文档中的旧编码回退 | 保留已经确认正确的行为，避免误删真实用户文本；各阶段排除计数基于当时剩余行并覆盖全量 CSV |
| 2026-09-22 | Claude Code 后续仅负责审查并提出发现，不直接修改代码或日志；Codex 按确认的产品决策修改并记录验证结果 | 审查方直接改写日志 | 让日志反映已确认的问题定义及可复现的修复验证，避免将建议当作已批准的契约变更 |
| 2026-09-22 | `detected_encoding` 暂不加入当前契约，记录为后续优化，后续实施前另行确认 | 本阶段新增编码识别结果字段 | 本阶段仅完成提交前收尾，保持当前数据契约与处理逻辑稳定 |
